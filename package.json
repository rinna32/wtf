{
  "scripts": {
    "lint:js": "eslint '**/*.{js,jsx}'",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "jest"
  },

  "pre-commit": [
    "lint",
    "format"
  ],

  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "pre-commit": "^1.2.2",
    "stylelint": "^16.24.0",
    "stylelint-config-standard": "^39.0.0"
  }
}
